📝 Домашнее Задание: Условия (if/else if/else)
Задание 1: "Проверка Числа" (Простое if/else)
Напиши программу, которая:
1.	Создает целочисленную (int) переменную с именем number и присваивает ей любое значение (например, 25 или -5).
2.	Использует конструкцию if/else, чтобы проверить:
o	Если число больше нуля (number > 0), вывести сообщение: "The number is positive."
o	В противном случае, вывести сообщение: "The number is zero or negative."
Пример Кода для Заполнения
#include <iostream>
using namespace std;

int main() {
    int number = /* Твое число */;

    // Напиши здесь свою конструкцию if/else

    return 0;
}
Задание 2: "Школьные Оценки" (if/else if/else)
Напиши программу, которая проверяет баллы (score) и присваивает буквенную оценку. Используй конструкцию if/else if/else.
1.	Создай переменную score (баллы) и присвой ей значение от 0 до 100.
2.	Программа должна вывести:
o	Если score от 90 до 100 (включительно), вывести: "Your grade is A (Excellent).".
o	Иначе, если score от 70 до 89, вывести: "Your grade is B (Good).".
o	Иначе, если score от 50 до 69, вывести: "Your grade is C (Satisfactory).".
o	Во всех остальных случаях (если меньше 50), вывести: "Your grade is F (Fail).".
Подсказка: Чтобы проверить, что число между 70 и 89, тебе не нужно использовать логические операторы, если ты делаешь проверки в правильном порядке. Компьютер остановится на первой if/else if, которая будет верна!
Пример Кода для Заполнения
#include <iostream>
using namespace std;

int main() {
    int score = 75; // Попробуй разные баллы: 95, 75, 40

    // Напиши здесь свою конструкцию if/else if/else

    return 0;
}
Задание 3: "Интерактивный Калькулятор" (if/else if/else)
Напиши программу, которая попросит пользователя ввести два числа и знак операции, а затем выполнит вычисление.
1.	Создай три переменные: num1 (тип double), num2 (тип double), и operation (тип char). Мы используем double, чтобы можно было работать с дробными числами.
2.	Попроси пользователя ввести числа и операцию, используя cout и cin.
3.	Используй конструкцию if/else if/else, чтобы проверить, какой знак находится в переменной operation:
o	Если operation == '+', вычисли и выведи сумму.
o	Иначе, если operation == '-', вычисли и выведи разность.
o	Иначе, если operation == '*', вычисли и выведи произведение.
o	Иначе, если operation == '/' (деление), вычисли и выведи частное.
o	Во всех остальных случаях, выведи сообщение об ошибке: "Error: Invalid operator."
Пример Кода для Заполнения
#include <iostream>
using namespace std;

int main() {
    double num1;
    double num2;
    char operation;

    // Шаг 1: Запрашиваем ввод у пользователя
    cout << "Enter first number: ";
    cin >> num1;

    // Дальше так же для других переменных

    // Шаг 2: Вычисления с помощью if/else if/else
    if (operation == '+') {
        cout << "Result: " << num1 + num2 << endl;
    } else if (operation == '-') {
        // Твой код для вычитания
    }  
     // Дальше также 
    } else {
        cout << "Error: Invalid operator." << endl;
    }

    return 0;
}

